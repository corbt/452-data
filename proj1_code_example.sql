-- Recommended way to start for Postgres

-- To run this file from command line type "psql -f proj1_code_example.sql"
-- To run in Sublime install the included SQL.sublime-build, restart Sublime and hit "CTRL+B".

-- \c p1 -- Connects to the database "p1"
\i proj1.sql -- Will run the code in proj1.sql to reset the database before each query

SELECT * FROM Product; -- Type your query here
